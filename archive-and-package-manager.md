# tar

Create or extract archive files.

tar -cvf backup.tar project/
# apt-get update

Refresh package repository metadata (/etc/apt/sources.list).

sudo apt-get update
# apt-get upgrade

Upgrade installed packages to newer versions.

sudo apt-get upgrade
